tmp/
*.test
*.out

# Generated by `go generate ./schema` from `codex app-server generate-json-schema`.
# Regenerate after a codex CLI bump. Not committed because (a) ~16k LOC churn on
# every bump drowns real diffs, and (b) it can be reproduced deterministically
# from the codex binary alone.
schema/v2_raw/
